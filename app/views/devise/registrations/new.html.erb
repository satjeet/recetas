<div class="row">
	<div class="col-md-6 col-md-offset-3">
		
		<h2>Registrarse</h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email, label:' Correo electronico ', required: true, autofocus: true,input_html:{class: 'form-control'} %>
    <%= f.input :password, label:' Contraseña ', required: true,input_html:{class: 'form-control'}, hint: ("#{@minimum_password_length} Caracteres minimos" if @minimum_password_length) %>
    <%= f.input :password_confirmation, label:' Confirmar Contraseña ', required: true,input_html:{class: 'form-control'} %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Registrarse",class: "btn btn-primary" %>
  </div>
  <br>

<% end %>

<%= render "devise/shared/links" %>

	</div>
	


</div>


